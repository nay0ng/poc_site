# 개인정보 검출·마스킹 PoC 사이트 기능명세서

## 1. 개요

### 1.1 목적

연금 관련 서류와 정보보호부서 제공 문서를 대상으로 다음 기능을 확인하기 위한 PoC 사이트를 구축한다.

1. 여러 형식의 문서에서 문자를 정상적으로 인식하는지 확인
2. 문서에 포함된 고유식별정보 4종을 검출하고 이미지에 마스킹하는지 확인
3. 전체 시험 파일의 OCR 처리시간과 평균 처리시간 확인
4. OCR 결과, 개인정보 검출 내역 및 마스킹 결과를 파일로 저장

### 1.2 별도 화면을 구성하지 않는 연계 기능

다음 기능은 전체 PoC 구성에서 사용하거나 연계할 수 있지만, 이번 사이트에는
별도의 전용 화면을 만들지 않는다.

| 기능 | 처리 방향 | 이번 사이트 범위 |
|---|---|---|
| 문서 분류 | 전용 화면은 만들지 않으며 실행·연계 방식은 별도로 확정 | 전용 화면 제외 |
| Key-Value 추출 | 별도 API로 호출할 예정이며 입력·출력 규격은 API 명세에서 정의 | 전용 화면 제외 |
| 바이릿지 기반 정형문서 인식·대사 | 새로 개발하지 않고 기존 바이릿지 기능 사용 | 신규 화면·기능 개발 제외 |

Key-Value 추출을 API로 제공한다는 사실만으로 화면이 불필요해지는 것은 아니다.
사용자가 결과를 확인하거나 수정해야 한다면 별도 화면이 필요할 수 있다. 현재는
그 요구가 없으므로 이번 기능명세에서는 API 연계만 언급하고 전용 화면은 제외한다.
추후 화면 요구가 생기면 API 응답을 표시·수정·저장하는 기능을 별도로 정의한다.

### 1.3 주요 사용자

- PoC 평가 담당자
- PoC 시연 담당자
- 개인정보 검출 결과를 확인하는 업무 담당자

## 2. 시험 대상

### 2.1 예상 문서

- KB증권 연금 관련 서류 약 40~50종
- 서류 종류별 개인정보 포함 샘플 10~20개
- 전체 예상 물량 약 400~500장
- 정보보호부서에서 추가로 제공하는 시험 문서

### 2.2 지원 예정 파일

- PDF
- JPG, JPEG
- PNG

여러 페이지로 구성된 PDF는 페이지별로 OCR을 처리하되, 결과에서는 원본 파일명과 페이지 번호를 확인할 수 있어야 한다.

실제 제공 파일에 다른 확장자가 포함되는 경우 지원 여부를 별도로 확인한다.

## 3. PoC 범위

### 3.1 기본 기능

- 문서 업로드 및 목록 확인
- OCR 전문인식 실행
- OCR 결과 JSON·TXT 저장 및 다운로드
- 고유식별정보 4종 검출
- 개인정보 검출 내역 생성
- 이미지 자동 마스킹
- 개인정보 검출 내역 저장
- 마스킹 이미지 저장
- 문서별 OCR 처리시간과 전체 평균 처리시간 확인

### 3.2 고유식별정보 4종

- 주민등록번호
- 외국인등록번호
- 여권번호
- 운전면허번호

PoC 사이트의 기본 처리 대상은 위 4종 전체로 한다.

### 3.3 추가 기능

추가 기능은 공식 PoC 평가 항목과 구분하여 제공하되, PoC 사이트의 기능 범위에 포함한다.

#### 개인정보 유형 관리

- 고유식별정보 4종은 기본 탐지 대상으로 제공한다.
- 사용자는 화면에서 전화번호와 이메일의 탐지·마스킹 여부를 추가로 선택할 수 있다.
- 활성화한 개인정보 유형만 검출 결과와 마스킹 결과에 포함한다.
- 추가 개인정보 유형은 전화번호와 이메일만 지원한다.
- 화면에서 선택한 유형은 서버의 개인정보 유형 설정값에 반영한다.

#### 수동 마스킹

- 이미지 Viewer에서 자동 마스킹 결과를 확인한다.
- 사용자가 새로운 마스킹 영역을 직접 추가한다.
- 추가한 영역을 선택하여 이동하거나 크기를 변경한다.
- 잘못 추가한 영역을 삭제하고 직전 편집을 실행 취소·다시 실행한다.
- 수정한 최종 마스킹 이미지를 저장한다.

### 3.4 명세 확정 상태

| 구분 | 상태 |
|---|---|
| OCR JSON·TXT 저장 | 요구사항 확인 |
| 고유식별정보 4종 검출·마스킹 | 요구사항 확인 |
| 마스킹 이미지·개인정보 검출 내역 제공 | 요구사항 확인 |
| 전화번호·이메일 선택 기능 | 요구사항 확인 |
| 수동 마스킹 | 영역 추가·이동·크기 변경·삭제·실행 취소·저장 포함 |
| 지원 파일 확장자 | 실제 제공 파일 확인 필요 |
| 개인정보 검출 내역 파일 형식 | 미정 |
| 마스킹 결과 저장 형식 | 1페이지는 PNG, 2페이지 이상은 텍스트 레이어가 없는 이미지 기반 PDF |



## 4. 전체 이용 흐름

```text
문서 업로드
→ OCR 및 개인정보 처리 실행
→ 문서별 처리 상태 확인
→ 마스킹 결과와 개인정보 유형별 건수 확인
→ 마스킹 이미지·검출 내역·OCR 결과 파일 다운로드
→ 전체 처리 결과와 평균 OCR 시간 확인
```

추가 기능은 위 기본 흐름에 이어서 자동 실행하지 않고, 사용자가 필요한 기능을
별도 메뉴에서 선택하여 실행한다.

## 5. 화면 구성

### 5.1 문서 등록 화면

| 화면 항목 | 기능 |
|---|---|
| 파일 선택 | 한 개 또는 여러 개의 문서 선택 |
| 업로드 영역 | 선택한 문서를 사이트에 등록 |
| 업로드 목록 | 파일명, 형식, 파일 크기, 페이지 수 표시 |
| 목록 삭제 | 잘못 등록한 파일을 실행 전에 목록에서 제거 |
| 전체 삭제 | 등록한 파일 전체 제거 |
| 처리 실행 | 등록 문서의 OCR 및 개인정보 처리를 시작 |

### 5.2 처리 설정 영역

| 설정 | 기본값 | 기능 |
|---|---|---|
| 고유식별정보 | 4종 전체 | 주민등록번호·외국인등록번호·여권번호·운전면허번호 처리 |
| 추가 개인정보 | 화면에서 선택 | 전화번호와 이메일의 탐지·마스킹 여부 선택 |
| 체크섬 검증 | 사용 안 함 | 켜면 체크섬을 지원하는 번호의 유효성을 추가 확인 |

체크섬 검증은 기본적으로 사용하지 않는다. 따라서 실제 형식과 유사한 시험용 번호나 체크섬이 맞지 않는 번호도 개인정보 후보로 검출하여 마스킹할 수 있다.

여권번호와 운전면허번호처럼 체크섬 검증을 사용하지 않는 유형은 이 설정의 영향을 받지 않는다.


### 5.3 처리 현황 화면

| 표시 항목 | 설명 |
|---|---|
| 전체 파일 수 | 등록된 원본 파일 수 |
| 전체 페이지 수 | 실제 OCR 처리 대상 페이지 수 |
| 파일 입력 상태 | 정상, 지원하지 않는 형식, 빈 파일, 손상 파일 |
| 페이지 변환 상태 | 성공, 일부 페이지 오류, 실패, 미실행 |
| OCR 상태 | 대기, 처리 중, 성공, 실패, 미실행 |
| 개인정보 엔진 실행 상태 | 대기, 처리 중, 실행 완료, 실행 오류, 미실행 |
| 개인정보 검출 건수 | 선택한 개인정보 유형별 검출 건수. 0건도 정상 실행 결과임 |
| 결과 이미지 생성 상태 | 대기, 처리 중, 생성 완료, 생성 오류, 미실행 |
| 최종 처리 상태 | 처리 완료, 부분 처리, 입력 오류, OCR 실행 오류, 개인정보 엔진 실행 오류, 결과 이미지 생성 오류 |
| 품질 검수 상태 | 미검수, 검수 완료. 실제 OCR·검출·마스킹 정확성은 별도로 판정 |
| 진행률 | 전체 페이지 중 완료한 페이지 비율 |
| 단계별 성공·실패 건수 | 입력, 페이지 변환, OCR, 개인정보 처리, 마스킹 단계별 건수 |
| OCR 처리시간 | 문서 또는 페이지별 OCR 소요시간 |
| 평균 OCR 처리시간 | 정상 처리된 전체 페이지의 평균 OCR 소요시간 |

특정 문서가 실패해도 나머지 문서 처리는 계속 진행한다.

#### 최종 처리 상태 판정 기준

| 최종 처리 상태 | 판정 기준 |
|---|---|
| 처리 완료 | 모든 페이지에서 OCR, 개인정보 엔진 실행과 결과 이미지 파일 생성이 오류 없이 끝남 |
| 부분 처리 | 여러 페이지 중 일부 페이지만 기술적 오류가 발생하고 나머지 페이지는 처리됨 |
| 입력 오류 | 지원하지 않는 형식, 빈 파일, 손상 파일 또는 암호화된 파일로 처리를 시작하지 못함 |
| OCR 실행 오류 | 페이지 변환은 완료했지만 OCR 서버가 응답하지 않거나 정상 결과를 반환하지 못함 |
| 개인정보 엔진 실행 오류 | OCR은 완료했지만 OCR 결과 읽기 또는 개인정보 탐지 프로그램 실행 중 예외가 발생함 |
| 결과 이미지 생성 오류 | 개인정보 엔진은 실행됐지만 결과 이미지 파일을 만들거나 저장하지 못함 |

처리 상태는 시스템 실행 결과만 표시한다.
개인정보 검출·마스킹 정확도는 자동 판정하지 않으며, 사용자가 원본과 결과 이미지를 비교하여 확인한다.
검출 결과가 0건이어도 오류가 없다면 처리 완료로 표시한다.

### 5.4 문서 결과 화면

처리가 완료된 문서를 선택하면 마스킹 결과를 중심으로 보여주고, OCR 결과와
개인정보 검출 상세정보는 다운로드 기능으로 제공한다.

#### OCR 처리 결과

- OCR 성공·실패 상태
- OCR 결과 JSON 다운로드
- 현재 페이지 OCR TXT 다운로드

OCR 전체 텍스트, 단어 좌표와 신뢰도는 PoC 기본 화면에 표시하지 않는다.

#### 개인정보 검출 요약

- 개인정보 유형별 검출 건수
- 검출된 파일명과 페이지 번호
- 개인정보 검출 내역 파일 다운로드(형식 미정)

개인정보 원문과 상세 좌표는 기본 화면에 노출하지 않고 검출 내역 파일에
기록한다. 검출 내역의 개인정보 값은 보안 기준에 따라 일부 마스킹하여 제공할
수 있다.

#### 마스킹 결과

- 마스킹 이미지 표시
- 검출 개인정보 건수
- 마스킹 영역 수
- 마스킹 이미지 파일 다운로드

원본과 마스킹 결과 비교 화면은 기능 구현이 아직 미정이다. 결과 검수가
필요한 경우에만 원본 보기 기능을 별도로 제공한다.

### 5.5 결과 요약 화면

결과 요약 화면의 우선순위는 정보의 중요도와 PoC에서 반드시 확인해야 하는
정도를 뜻한다. 숫자가 작을수록 중요도가 높다.

#### 우선순위 1: 핵심 결과 — 반드시 표시

PoC의 기본 평가 결과이므로 결과 요약 화면에서 바로 확인할 수 있어야 한다.

| 표시 항목 | 설명 | 권장 표시 방식 |
|---|---|---|
| 전체 파일·페이지 수 | 등록한 파일 수와 변환된 전체 페이지 수 | 상단 요약 카드 |
| OCR 성공률 | 전체 페이지 중 OCR이 성공한 페이지 수와 비율 | `성공 페이지 / 전체 페이지 (비율)` |
| 평균 OCR 시간 | OCR 성공 페이지 기준 평균 처리시간 | 초 단위 요약 카드 |
| 개인정보 검출 문서 수 | 개인정보가 한 건 이상 검출된 문서 수 | 문서 수 요약 카드 |
| 유형별 검출 수 | 최종 탐지 정책을 통과한 주민등록번호·외국인등록번호·여권번호·운전면허번호별 검출 건수와 검출 문서 수 | 4개 유형을 모두 표시하는 표 |
| 마스킹 결과 수 | 마스킹 이미지가 정상 생성된 문서·페이지 수 | `생성 페이지 / 대상 페이지` |

유형별 검출 수 표에는 검출되지 않은 유형도 `0건`으로 표시한다. 이 수치는
탐지기가 찾은 후보의 집계이며, 정답 데이터와 비교한 정확도나 문서에 실제로
포함된 개인정보 총개수를 의미하지 않는다.

#### 우선순위 2: 장애 분석 정보 — 오류 발생 시 중요

정상 처리 결과보다 중요도는 낮지만, 기술적 실패가 발생했을 때 원인을 설명하고
재처리 대상을 정하기 위해 필요하다. 기본 화면에는 전체 기술 오류 건수를 요약하고,
아래 목록은 `실패 상세 보기`와 같은 별도 영역에서 확인한다.

| 표시 항목 | 설명 |
|---|---|
| 입력·변환 실패 목록 | 파일명, 페이지 번호와 실패 사유 |
| OCR 실패 목록 | OCR에 실패한 파일명, 페이지 번호와 실패 사유 |
| 개인정보 엔진 실행 오류 목록 | OCR 완료 후 개인정보 탐지 프로그램 실행 중 예외가 발생한 문서와 사유 |
| 결과 이미지 생성 오류 목록 | 개인정보 엔진 실행 후 결과 파일을 만들지 못한 문서와 사유 |
| 부분 처리 목록 | 일부 페이지만 기술적 오류가 발생한 문서와 페이지별 상태 |

#### 우선순위 3: 품질 평가 정보 — 검수 조건이 준비된 경우 표시

| 표시 항목 | 설명 |
|---|---|
| 품질 검수 결과 | 사람이 확인하거나 정답 데이터와 비교한 정상·미탐·오탐·부분·과대 마스킹 결과 |

품질 검수 결과는 프로그램 실행만으로 자동 산출할 수 없다. 정답 데이터가 준비되거나
사용자가 원본과 결과를 직접 검수하는 기능이 확정된 경우에만 결과 요약에 추가한다.

### 5.6 전화번호·이메일 탐지 설정 및 결과

- 사용자는 화면에서 전화번호와 이메일의 탐지·마스킹 여부를 선택할 수 있다.
- 고유식별정보 4종은 기본 탐지 대상으로 표시한다.
- 전화번호와 이메일은 추가 개인정보 유형으로 각각 선택할 수 있다.
- 화면에서 선택한 유형을 서버의 개인정보 유형 설정값에 반영한다.
- 활성화한 유형만 개인정보 검출 건수와 마스킹 결과에 포함한다.

전화번호와 이메일은 추가 탐지 항목으로 제공한다. 화면에서 선택한 항목을 서버의
개인정보 탐지 유형 설정에 반영하고, 선택한 유형만 검출·마스킹한다.

### 5.7 수동 마스킹 화면

| 화면 항목 | 기능 |
|---|---|
| 이미지 Viewer | 원본 또는 자동 마스킹 이미지 표시 |
| 화면 확대·이동 | 작은 글자와 마스킹 영역을 정확히 확인할 수 있도록 확대·축소 및 화면 이동 |
| 영역 추가 | 마우스로 마스킹 사각형 추가 |
| 영역 선택 | 편집할 자동 또는 수동 마스킹 영역 선택 |
| 영역 이동 | 선택한 마스킹 영역의 위치 변경 |
| 크기 변경 | 선택한 마스킹 영역의 가로·세로 범위 조정 |
| 영역 삭제 | 잘못 생성되거나 불필요한 마스킹 영역 제거 |
| 실행 취소·다시 실행 | 직전 편집 작업을 취소하거나 다시 적용 |
| 편집 초기화 | 현재 문서의 수동 편집 내용을 자동 마스킹 결과 상태로 되돌림 |
| 결과 저장 | 자동·수동 마스킹을 반영하여 1페이지는 PNG, 2페이지 이상은 이미지 기반 PDF로 저장 |

자동 마스킹 영역과 사용자가 추가한 영역은 화면에서 구분할 수 있어야 한다. 편집은
원본 이미지 위의 영역 좌표를 수정하는 방식으로 처리하고, 저장할 때 최종 마스킹
이미지를 생성한다. 영역 정보에는 페이지 번호, 사각형 좌표와 자동·수동 구분값을
포함한다. 최종 이미지 확장자와 영역 정보의 상세 JSON 구조는 구현 단계에서 확정한다.

## 6. 상세 기능 목록

| 기능 ID | 기능명 | 주요 내용 | PoC 구분 |
|---|---|---|---:|
| POC-001 | 다중 문서 업로드 | PDF와 이미지 파일을 여러 개 등록 | 필수 |
| POC-002 | 다중 페이지 처리 | PDF의 각 페이지를 나누어 OCR 처리 | 필수 |
| POC-003 | 업로드 목록 관리 | 파일명·형식·크기·페이지 수 확인 및 삭제 | 필수 |
| POC-004 | OCR 실행 | 등록 문서를 OCR 서버로 전송하여 텍스트 추출 | 필수 |
| POC-005 | OCR 결과 저장 | OCR 결과를 JSON과 TXT로 저장·다운로드 | 필수 |
| POC-006 | 개인정보 유형 적용 | 고유식별정보 4종을 기본 처리하고, 전화번호·이메일은 화면 선택에 따라 처리 | 필수 |
| POC-007 | 체크섬 검증 선택 | 체크섬 검증 사용 여부 선택, 기본값은 사용 안 함 | 선택 |
| POC-008 | 개인정보 검출 | 선택한 개인정보 유형의 문자열과 위치 검출 | 필수 |
| POC-009 | 검출 결과 요약 | 고유식별정보 4종별 검출 건수·검출 문서 수와 파일·페이지 정보 표시, 미검출 유형도 0건으로 표시 | 필수 |
| POC-010 | 개인정보 내역 저장 | 검출 결과 저장·다운로드, 파일 형식 미정 | 필수 |
| POC-011 | 자동 마스킹 | 개인정보 위치를 검은 영역으로 처리 | 필수 |
| POC-012 | 결과 이미지 확인·저장 | 마스킹 이미지를 화면에 표시하고 1페이지는 PNG, 2페이지 이상은 이미지 기반 PDF로 다운로드 | 필수 |
| POC-013 | 문서 이동 | 이전·다음 문서 및 파일 목록 직접 선택 | 필수 |
| POC-014 | 단계별 실행 상태 표시 | 입력·페이지 변환·OCR·개인정보 엔진·결과 이미지 생성 상태를 각각 표시 | 필수 |
| POC-015 | 처리시간 측정 | 문서별 OCR 시간과 전체 평균 OCR 시간 계산 | 필수 |
| POC-016 | 실행 오류 내역 제공 | 기술적 오류가 발생한 단계, 파일·페이지와 오류 원인 표시 | 필수 |
| POC-017 | 전체 결과 요약 | 전체 파일·페이지 수, OCR 성공률·평균 시간, 개인정보 검출 문서 수, 고유식별정보 4종별 검출 건수·검출 문서 수, 마스킹 결과 수 표시 | 필수 |
| POC-018 | 일괄 결과 제공 | 전체 결과와 요약 데이터를 일괄 제공, 제공 형식 미정 | 필수 |
| ADD-001 | 개인정보 유형 선택 | 화면에서 전화번호와 이메일의 탐지·마스킹 여부 선택 | 추가 |
| ADD-002 | 추가 개인정보 결과 | 활성화한 전화번호·이메일의 검출·마스킹 내역 표시 | 추가 |
| ADD-003 | 수동 마스킹 Viewer·영역 추가 | 자동 마스킹 이미지 표시, 확대·축소·화면 이동, 자동·수동 영역 구분 및 신규 마스킹 영역 생성 | 추가 |
| ADD-004 | 수동 마스킹 영역 편집 | 영역 선택·이동·크기 변경·삭제·실행 취소·다시 실행·초기화 | 추가 |
| ADD-005 | 수동 마스킹 저장 | 수정한 마스킹 이미지와 영역 정보 저장 | 추가 |

`PoC 구분`은 개발 순서가 아니라 해당 기능이 이번 PoC에서 어떤 성격을 가지는지
나타낸다.

| PoC 구분 | 판단 기준 |
|---|---|
| 필수 | PoC 핵심 평가와 기본 처리 흐름을 실행하는 데 반드시 필요한 기능 |
| 선택 | 기본 처리는 가능하지만 실행 시 사용 여부를 선택할 수 있는 기능 |
| 추가 | PoC 핵심 평가 외에 솔루션의 부가 기능으로 제공하거나 시연하는 기능 |

실제 개발 순서가 필요하면 `P0·P1·P2` 등의 개발 우선순위를 별도 항목으로
관리한다. 결과 요약 화면의 `우선순위 1~3`은 각 결과 정보의 중요도와 필수 표시
여부를 뜻한다.

## 7. 저장 및 다운로드 결과

처리 결과 확인과 재현을 위해 공통 실행 결과는 저장한다. 품질 검수와 추가 기능
결과는 해당 기능을 실제로 수행한 경우에만 저장한다. 세부 파일 형식과 일괄 제공
방식은 구현 단계에서 확정한다.

### 7.1 문서별 결과

- OCR 결과 JSON
- OCR 텍스트 TXT
- 개인정보 검출 내역 파일: 유형, 페이지, 검출 위치, 마스킹 여부 포함(형식 미정)
- 최종 마스킹 결과 파일(1페이지 PNG, 2페이지 이상 이미지 기반 PDF)
- 문서별 처리 상태와 OCR·개인정보 처리시간

### 7.2 전체 결과

- 문서별 단계 실행 상태와 최종 처리 상태
- 입력·페이지 변환·OCR·개인정보 엔진·결과 이미지 생성 단계별 완료·오류 목록
- 별도 품질 검수를 수행한 경우 미탐·오탐·부분·과대 마스킹 결과
- 문서별·페이지별 OCR 처리시간
- 전체 평균 OCR 처리시간
- 개인정보 유형별 검출 건수
- 마스킹 이미지 생성 결과
- 실행 시 적용한 개인정보 유형과 탐지 정책 설정
- 문서별 결과 파일의 일괄 다운로드(제공 형식 미정)

### 7.3 추가 기능 결과

- 실행 시 적용한 전화번호·이메일 선택 설정
- 전화번호·이메일 검출 내역
- 수동 마스킹 영역 정보와 최종 이미지

개인정보 검출 내역에는 실제 개인정보 원문이 포함될 수 있으므로 접근 권한과 보관
기간을 적용해야 한다. 화면과 다운로드 파일에는 원문 대신 마스킹된 값과 유형별
집계값을 사용하는 것을 기본으로 한다.

## 8. 오류 및 예외 처리

### 8.1 시스템 실행 오류

| 상황 | 처리 방식 |
|---|---|
| 지원하지 않는 파일 | `입력 실패`로 표시하고 지원 형식 안내 |
| 빈 파일·손상 파일·암호화 PDF | `입력 실패`로 표시하고 OCR은 실행하지 않음 |
| PDF 전체 페이지 변환 실패 | `페이지 변환 실패`로 표시하고 OCR은 실행하지 않음 |
| PDF 일부 페이지 변환 실패 | 문서를 `부분 처리`로 표시하고 변환 가능한 페이지는 계속 처리 |
| OCR 서버 연결 실패 | `OCR 실패`로 표시하고 재시도 가능 상태로 제공 |
| OCR 응답 실패·빈 결과 | `OCR 실패`로 표시하고 응답 코드와 실패 사유 기록 |
| OCR JSON 또는 token·좌표 읽기 중 예외 발생 | `개인정보 엔진 실행 오류`로 표시하고 OCR 결과는 유지 |
| 개인정보 탐지 프로그램 실행 중 예외 발생 | `개인정보 엔진 실행 오류`로 표시하고 오류 원인 기록 |
| 개인정보가 검출되지 않음 | 엔진이 정상 종료됐다면 `실행 완료·0건`으로 표시 |
| 원본 이미지 또는 bbox 연결 중 예외 발생 | `결과 이미지 생성 오류`로 표시하고 개인정보 검출 결과는 유지 |
| 마스킹 이미지 파일 생성·저장 중 예외 발생 | `결과 이미지 생성 오류`로 표시하고 개인정보 검출 결과는 유지 |
| 수동 마스킹 저장 실패 | 편집 중인 영역을 유지하고 다시 저장할 수 있도록 안내 |

오류 메시지와 서버 로그에는 전체 개인정보 문자열이 불필요하게 노출되지 않도록 한다.

### 8.2 개인정보 품질 검수 결과(조건부)

아래 항목은 프로그램의 실행 오류가 아니라 개인정보 검출·마스킹 품질 평가 결과다.
정답 데이터가 있거나 사람이 원본과 결과를 검수한 경우에만 기록한다.

| 품질 구분 | 판단 기준 |
|---|---|
| 미탐 | 원본에 개인정보가 있지만 검출·마스킹되지 않음 |
| 오탐 | 개인정보가 아닌 값이 검출·마스킹됨 |
| 부분 마스킹 | 개인정보 문자열 또는 영역의 일부만 가려짐 |
| 과대 마스킹 | 개인정보 범위를 넘어 주변의 불필요한 영역까지 가려짐 |
| 정상 | 필요한 개인정보 영역이 누락이나 과대 영역 없이 가려짐 |

검수 결과에는 파일명, 페이지 번호, 개인정보 유형, 품질 구분과 검수 의견을 저장한다.
별도의 웹 검수 화면은 필수로 두지 않으며, 정답 데이터 제공 여부와 검수 방법이
확정되면 추가한다.

## 9. 기능 검수 기준

- 지원 형식의 문서를 한 개 또는 여러 개 업로드할 수 있다.
- 40~50종, 총 400~500장 규모의 시험 문서를 처리할 수 있다.
- PDF의 각 페이지가 분리되어 처리되고 원본 파일·페이지 번호가 유지된다.
- OCR 성공 여부를 확인하고 결과를 JSON·TXT로 내려받을 수 있다.
- 고유식별정보 4종을 기본값으로 검출·마스킹한다.
- 체크섬 검증의 초기 상태는 `사용 안 함`이다.
- 원본 이미지의 개인정보 위치에 마스킹이 적용된다.
- 개인정보 검출 내역을 파일로 내려받을 수 있다. 파일 형식은 추후 확정한다.
- 마스킹 이미지를 화면에서 확인하고 내려받을 수 있다.
- 특정 파일이 실패해도 전체 처리가 중단되지 않는다.
- 입력·페이지 변환·OCR·개인정보 엔진·결과 이미지 생성 중 어느 단계에서 기술적 오류가 발생했는지 확인할 수 있다.
- 여러 페이지 중 일부에만 기술적 오류가 발생한 문서는 `부분 처리`로 표시된다.
- 개인정보 탐지 프로그램이 정상 종료되고 결과가 0건이면 `실행 완료·0건`으로 표시된다.
- 미탐·오탐·부분 마스킹·과대 마스킹은 정답 비교 또는 사람 검수 결과로 별도 기록한다.
- 문서별 처리시간과 전체 평균 OCR 처리시간을 확인할 수 있다.
- 문서별 처리 결과 파일을 일괄 제공할 수 있다. 제공 형식은 추후 확정한다.
- 화면에서 선택한 전화번호와 이메일의 검출·마스킹 결과를 확인할 수 있다.
- 이미지 Viewer에서 마스킹 영역을 추가·이동·크기 변경·삭제할 수 있고, 실행 취소·다시 실행 후 결과를 저장할 수 있다.

## 10. 개발 전 확인사항

| 확인 항목 | 확인 내용 |
|---|---|
| 실제 제공 확장자 | PDF, JPG/JPEG, PNG, BMP 외 형식 포함 여부 |
| 파일 크기 | 파일당 최대 크기와 전체 업로드 용량 |
| 처리 방식 | 400~500장 일괄 처리 또는 여러 차례 분할 처리 여부 |
| 평균 속도 기준 | 파일당 평균, 페이지당 평균 또는 두 지표 모두 제출할지 여부 |
| 파일 보관기간 | 원본·OCR 결과·마스킹 결과의 삭제 시점 |
| 추가 개인정보 기본값 | 전화번호와 이메일의 화면 기본 선택 여부 |
